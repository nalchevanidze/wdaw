{
  "name": "waw",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start":"concurrently --kill-others 'tsc -b -w' 'webpack serve'",
    "test": "jest",
    "clean": "rm -rf ./packages/*/.tsbuildinfo ./packages/*/dist ./packages/*/node_modules",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "concurrently": "^8.0.1",
    "jest": "^29.5.0",
    "lerna": "^6.6.2",
    "swc-loader": "^0.2.3",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.4",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.15.1"
  }
}
